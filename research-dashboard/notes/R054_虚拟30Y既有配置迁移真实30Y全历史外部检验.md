# R054 虚拟 30Y 既有配置迁移真实 30Y 全历史外部检验

## 基本信息

- 研究 ID：R054
- Owner：`/root/research_real30y_allconfigs`
- 创建时间：2026-07-12 Asia/Shanghai
- 状态：`Research-Only`
- 关联看板项：`research-dashboard/research_backlog.md` R054

## 假设与冻结条件

- 假设：现有虚拟 30Y 配置若仅替换为真实 `511090.SH` 或 `511130.SH`，其风险均衡机制可能迁移到真实长久期 ETF，并降低债券名义资金占用。
- 冻结内容：所有源配置的策略算法、参数、资产池、调仓、费用、执行参数和验收口径；只允许替换 `CN_ETF:511260_3X.SH` 的资产定义、名称、代码及 universe 引用。
- 外部测试：每只真实 ETF 上市以来、与配置其余 required symbols 的完整共同窗口；同窗运行原虚拟配置。
- 三滑点：`default`、`stress`、`dynamic_participation`。
- 首批六配置：`baseline_r0_domestic_equal_weight.yaml`、`baseline_r1_domestic_rolling.yaml`、`baseline_r1_domestic_ewma.yaml`、`baseline_risk_parity_lw_cov.yaml`、`baseline_risk_parity_hrp.yaml`、`baseline_risk_parity_gerber.yaml`。
- 限制：R051/R052 已查看重叠行情，本研究不是纯净未见 OOS；用户明确覆盖本次固定日期切分，但不放宽未来研究规则。不得看结果后调参、筛掉配置或择优 ETF。
- 资格：真实 ETF 截至既有训练截止不足三年，最终只能形成 `Research-Only` 迁移证据，不能输出正式平台配置或直接实盘晋级。

## 实现范围

- 不修改策略源码或注册。
- 研究派生配置和 artifacts 使用新的时间戳目录，不覆盖历史产物。
- 临时批处理脚本在任务结束前删除。

## 验证进度

- 数据预检发现完整 inventory 所需 `515080` 最后日期为 `2026-06-24`，相对运行日 `2026-07-12` 已超过 7 个自然日。
- 已运行 `sync_all_market_data.py`，但该入口固定同步的 12 个 universe assets 不含 `515080`。
- 随后对 `baseline_r1_user_holdings.yaml` 执行带 `--fetch` 的定向同步；Tencent 返回解析错误，EastMoney/BaoStock 返回 `NoneType.strftime`，最终同步器因 `date - NaTType` 抛出 `TypeError`，同步失败。
- 按根规则，数据同步或对齐失败必须停止，不得回测、输出配置或形成绩效结论。因此六经典配置和完整 inventory 均未运行。

## 精确命令

```powershell
.\env\python.exe platform\scripts\sync_all_market_data.py
.\env\python.exe platform\scripts\sync_platform_data.py --config configs\baseline_r1_user_holdings.yaml --fetch
```

## 结论

- 推荐：`Research-Only`。同步器已做最小可复用修复，`515080` 刷新至 `2026-07-10`，完整 16-code 审计通过。
- 已完成 56 配置 × 2 ETF 窗口 × 2（真实/虚拟）× 3 滑点，共 `672` runs。
- 两只真实 ETF 在全部配置和三滑点下 Sharpe 均低于同窗虚拟参照，胜率 `0/56`；不允许直接实盘晋级。
- 标准报告：`platform/reports/r054_real_30y_all_config_external_test_report.md`。
