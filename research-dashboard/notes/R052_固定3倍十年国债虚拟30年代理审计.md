# R052 固定 3× 十年国债虚拟 30 年代理审计

## 基本信息

- 研究 ID：R052
- 课题：固定 3× 十年国债虚拟 30Y 的代码、估值与期限风险代理审计
- Owner：`/root/research_virtual30y`
- 创建时间：2026-07-11 Asia/Shanghai
- 状态：`Research-Only`
- 关联看板项：`research-dashboard/research_backlog.md`
- 标准实验报告：`platform/reports/r052_virtual_30y_proxy_semantics_audit.md`

## 假设与来源

- 假设：固定 `3.0` 倍的 `511260` 后复权日收益并不等价于真实 30 年国债 ETF；当前合成行情若进入 ETF 执行路径，会污染成交量、成交额、涨跌停、整手、滑点及实盘下单语义。
- 来源：看板 R052 所列 SEC 与 IMF 官方资料；后续报告补充权威一手来源。
- 主要风险：久期比变化、凸性、曲线非平行移动、每日复利、融资费用与真实 ETF 流动性均未被固定倍数完整建模。
- 不应合入的条件：数据新鲜度或对齐失败；共同训练历史不足三年；不可交易字段污染或重复杠杆未消除；代理血缘不可追溯。

## 冻结条件

- 训练样本截止：`2025-06-30`
- 最终测试起点：`2025-07-01`
- 冻结内容：本任务先做诊断，不选择可提交候选；任何代理规范必须包含 `source`、公式版本、输入快照 hash 和不可交易标志。
- 冻结前不得使用：最终测试样本表现、指标或排序。

## 实现范围

- 修改文件：研究笔记、中文审计报告、看板与历史摘要。
- 新增配置：无。
- 新增或修改策略注册：无。
- 临时脚本：不得保留。
- 清理动作：不覆盖历史 artifacts，不修改他人未提交工作。

## 验证命令

以下命令会从标准报告中逐字提取完整的 `powershell` 验证代码块并执行；报告内代码块已展开四条 CSV 的日期/缺失值检查、截至 `2025-06-30` 的收益与滚动波动统计、OHLC/流动性/SHA-256 检查及全部 YAML 的重复 `vol_multipliers` 扫描，不含占位符：

```powershell
$report = Get-Content -Raw -Encoding utf8 platform/reports/r052_virtual_30y_proxy_semantics_audit.md
$validationBlock = [regex]::Match($report, '(?s)## 验证命令\s*```powershell\s*(.*?)\s*```').Groups[1].Value
if ([string]::IsNullOrWhiteSpace($validationBlock)) { throw '未找到 R052 powershell 验证代码块' }
Invoke-Expression $validationBlock
```

数据最新日为 `2026-07-10`；训练期真实 30Y 共同史不足三年，且发现继承虚假流动性字段、OHLC 约束破坏和两项重复 `vol_multipliers`，故停止组合级实证。
